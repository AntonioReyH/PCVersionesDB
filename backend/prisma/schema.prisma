generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// Estructura v1: Solo permite IPv4
model IPRecord {
  id        Int      @id @default(autoincrement())
  addressV4 String   @unique
  createdAt DateTime @default(now())
}
